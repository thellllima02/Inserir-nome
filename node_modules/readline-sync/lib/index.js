const fs = require("fs");
const readlineSync = require("readline-sync");

const input = readlineSync.question("Digite algo: ");

fs.appendFile("arquivo.txt", input, function (err) {
  if (err) throw err;
  console.log("O texto foi adicionado ao arquivo!");
});
